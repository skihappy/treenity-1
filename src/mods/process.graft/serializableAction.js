const deserializedAction=

const serializableAction=(metaModel, serializedActions)=>{
    return metaModel.action((self)=>{
        Object.assign(self,serializedActions)
        const createSerializedAction=(name,serializedAction)=>{

        }
        return {
            actionNames.reduce((actions,actionName)=>)
        }
    })
}